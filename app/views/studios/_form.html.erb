<%= app_form_with(model: studio) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :website_url %>
  <%= f.input :phone %>

  <%= render "partials/form_save_or_delete", record: studio, f: f %>
<% end %>
